services:
  aski-water-watch:
    build: .
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - aski-data:/data
    ports:
      - "127.0.0.1:8081:8081"

volumes:
  aski-data:
